apiVersion: v1
kind: Service

metadata:
  name: flask-service
  namespace: production

spec:
  type: LoadBalancer

  selector:
    app: flask-app

  ports:
  - name: http
    port: 80
    targetPort: 5000